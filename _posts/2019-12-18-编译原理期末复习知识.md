---
layout:     post
title:      "编译原理期末复习知识"
subtitle:   ""
date:       2019-12-18
author:     "LiuBiyongge"
header-img: "img/in-post/all.jpg"
tags:
  - 编译原理
typora-root-url: ..
---



# 编译原理期末复习知识

[TOC]

> 根据考点详细编写，知识概况广(对于深刻掌握知识，也应该记录)

## 考点

![重点IMG_9810](/img/in-post/重点IMG_9810.JPG)

<img src="/img/in-post/重点IMG_9813.JPG" alt="重点IMG_9813" style="zoom:50%;" />

![重点IMG_9818](/img/in-post/重点IMG_9818.JPG)

## 引论

### 什么叫编译程序

- 翻译程序: 把某一种语言程序转换成另一种语言程序，而后者在逻辑上与前者等价。
- 解释程序: 读入源程序，并不把源程序转换为目标程序，而是边解释边执行源程序本身。

### 编译程序概述

- 词法分析  

> 任务: 输入源程序，输出单词符号
>
> 描述: 正规式和有限自动机
>
> 线性分析

- 语法分析

> 任务: 识别出各类语法单位，判断输入串在语法上是否构成真正的程序。
>
> 描述: 上下文无关文法
>
> 层次分析

- 语义分析及中间代码生成

> 对语法单位进行语义分析并把它翻译成一定形式的中间代码。
>
> 第一步，静态语义检查 第二部，中间代码的翻译
>
> 描述：属性文法

- 优化

> 对中间代码进行优化处理

- 目标代码生成

> 把中间代码转换为目标代码

### 编译程序的结构

- 词法分析器
- 语法分析器
- 语义分析及中间代码生成器
- 优化器
- 目标代码生成器
- 表格管理     

> 符号表，编译各阶段都涉及构造、查找、更新有关表格

- 出错管理

> 语法错误：不符合语法规则或词法规则的错误
>
> 语义错误：说明错误、作用域错误、类型不一致

---

编译前端：与目标机无关

编译后端：与目标机有关

## 高级语言及其语法描述

### 程序语言的定义

- 语法: 语法包括词法规则和语法规则

程序语义的基本功能是`描述数据`和｀对数据的运算｀

### 程序语言的语法描述

> 指语法规则

<img src="/img/in-post/image-20191218184928800.png" alt="image-20191218184928800" style="zoom: 50%;" />

<img src="/img/in-post/image-20191219133025980.png" alt="image-20191219133025980" style="zoom:50%;" />

- 文法 描述语言的语法结构的形式规则（即语法规则）。$(V_T,V_N,S,P)$
- 上下文无关文法

- $\rightarrow$ 产生式 $\Rightarrow$ 直接推出

#### 题型

- 文法 $\Longleftrightarrow$ 语言

1. 文法 $\rightarrow$ 语言

列举法

2. 语言 $\rightarrow$ 文法

1. 列举 写出最简单一个语言
2. 拆分 把次数相同的拆分为一个整体
3. 构造
4. 检查

$L = \{1^{n}0^{m}1^{m}0^{n}|n,m\geq 0\}$
$$
L\rightarrow 1L0|@|1A0 \\
A \rightarrow 0A1 | @
$$


- 最左推导 最右推导
- 二义性证明 找出同一个句子的两棵不同的语法树

利用替换优先顺序产生不同的语法树

## 词法分析

### 词法分析器的设计

- 状态转换图

在转换图中，结点代表状态，用圆圈表示。状态之间用箭弧连结。箭弧上的标记（字符）代表在射出结点（即箭弧始结点）状态下可能出现的输入字符或字符类。

状态转换图中有一个初态，至少一个终态。

终态上打个*号，表示多读进了一个不属于标识符部分的字符，应把它退还给输入串

![image-20191219141835414](/img/in-post/image-20191219141835414.png)

### 正规表达式和有限自动机

- 正规式 先"*",次"$\cdot $ "最后"|"
- 正规集

- DFA $(S,\sum,\delta,s_0,F)$

> S是一个有限集，它的每个元素称为一个状态。
>
> S是一个有穷字母集，它的每个元素称为一个输入字符。
>
> δ是一个从S x S至S的单值部分映射 δ(s,a）=s’。意味着：当现行状态为s、输入字符为a时，将转换到下一个状态s’。我们称s’为s的后继状态。
>
> $s_0$，是唯一的初态。
>
> F，是一个终态集（可空）

- NFA $(S,\sum,\delta,S_0,F)$

>S 同DFA中S的定义
>
>Σ 同DFA中Σ的定义
>
>δ是一个从S x Σ*到S的子集的映照
>
>  δ：S x Σ*→$2^s$
>
>$S_0$,是一个非空的初态集；
>
>F ，是一个终态集（可空） 

- 空闭包

定义1：集合I的ε-闭包

令I是一个状态集的子集，定义

ε-closure（I）为：

1）若s∈I，则s∈ ε-closure（I） ；

2）若s∈I，则从s出发经过任意条ε弧能够到达的任何状态都属于ε-closure（I）。    

状态集ε-closure（I）称为I的ε-闭包

 $I_a=ε-closure(J)，其中J = ∪_{s\in I}δ(s,a)$

- NFA $\rightarrow$ DFA

1. 引进新的初态节点X，终态节点Y(如果只有一个初态或终态不用引进)

   从X到S0 中任意状态节点连一条**e**箭弧**，**从F中任意状态节点连一条箭弧到Y

2. 对M的状态转换图施行如下图替换，其中k是新引入的状态 

![image-20191219143313532](/img/in-post/image-20191219143313532.png)

3. 子集法

   a. 先求ε-closure（X)，放到第一个元素

   b. 再求$I_a, I_b$

   c. 然后依次把未出现元素放到第一个元素，以此类推

   d. 重新命名状态子集

- 正规文法 $\Longleftrightarrow$ DFA



- 正规式 $\Longleftrightarrow$  DFA 
  1. 正规式 $\Longleftrightarrow$  NFA

![img](https://pic2.zhimg.com/80/v2-898e5775bb068df21403c0baf7ee9035_hd.jpg)

​				2. NFA $\Longleftrightarrow$ DFA

- DFA化简[^1]

1. 划分为终态集和非终态集
2. 对于某一子集$I^{(i)}$ 若存在输入字符a,使得$I_a^{(i)}$ 不全包含于某个子集中，就划分$I^{(i)}$,直到不可以划分为止
3. 令子集中某个状态代替这个子集，到达子集中其它状态的弧都导入这个状态中，删除其它状态.

#### 题型

- 正规式 $\Longleftrightarrow$  正规集

- 正规式 $\rightarrow$ 最简DFA

- 正规式 $\rightarrow$ 文法

## 语法分析---自上而下分析

### LL1(1)分析法

- 消除左递归

> 直接左递归
>
> 若：P→Pa| b，
>
> 则可改写为：P →b P’          P’ →aP’| ε

>   一般左递归
>
> 非终结符排序
>
> 把在非终结符之前的非终结符的表达式按排序后顺序代入这个终结符表达式（只代入1次，比如B代入C时，又产生了A,则不再替换A）,然后消去直接左递归
>
> ![image-20191219155843459](/img/in-post/image-20191219155843459.png)
>
> 消去从开始符号出发永远不能到达的非终结符的产生规则

- 消除回溯、提取左因子
- First

<img src="/img/in-post/image-20191219160920243.png" alt="image-20191219160920243" style="zoom: 80%;" />

  - Folow

<img src="/img/in-post/image-20191219161254419.png" alt="image-20191219161254419" style="zoom:80%;" />

- 预测分析表的构造

  >  对每个文法符号$X\in V_T\cup V_N $  ,构造FIRST(X)
  >
  >  若$X \in V_T$，则FIRST(X)={X}
  >
  > 若$X \in V_N$，，且有产生式X->a…,则把a加入到FIRST(X)中；若X->ε也是产生式，则把ε加入到FIRST(X)中。
  >
  > 若X->Y…是一个产生式且$Y \in V_N$，则把FIRST(Y)中所有非ε元素都加入到FIRST(X)中。若X->Y1Y2…Yk是一个产生式，Y1,…Yi-1都是非终结符，且对于任何$1\leq j\leq i-1$, FIRST(Yj)中都含有ε，则把FIRST(Yi)中的所有非ε元素都加入到FIRST(X)中。特别地，若所有FIRST(Yj)均含有ε ，则把ε加入到FIRST(X)中。
  >
  > 连续使用上述规则，知道FIRST集合不再扩大为止。

  > 对文法的每个非终结符A构造FOLLOW(A)
  >
  > 对于文法的开始符号S, #∈FOLLOW(S)
  >
  > 对于A→ αBβ的产生式，则把FIRST(β)\\{ε}放入FOLLOW (B)中
  >
  > 对于A→ αB或A→ αBβ，其中β=>ε， 则将FOLLOW (A)放入FOLLOW(B)中

  > ![image-20191219163546085](/img/in-post/image-20191219163546085.png)

#### 题型

  - 消除左递归
  - 求First和Follow
  - 判定文法是否为LL(1)文法

## 语法分析---自下而上分析

### 自下而上分析基本问题

- 规范规约

  令G是一文法，S是文法的开始符号，$\alpha \beta\delta$是文法G的一个句型：

  如果有$S\Rightarrow \alpha A \delta$且$A\Rightarrow^{*}\beta$，则称$\beta$是句型$\alpha \beta\delta$相对于非终结符A的短语。

  若有A$\Rightarrow \beta$，则称$\beta $是句型$\alpha \beta\delta$ 相对A$\Rightarrow \beta$的直接短语。一个句型的最左直接短语称为该句型的句柄。

  ---

  判断方法，画树

   E $\rightarrow $T|E+T

   T$\rightarrow $ F|T*F

   F$\rightarrow $ i | (E) 

  Example: 句型$i_1 * i_2 + i_3$

  ![image-20191219195653381](/img/in-post/image-20191219195653381.png)

  1. 短语 以非叶子结点为根的子树的叶子结点的排列

  > $i_1 \\ i_2 \\ i_3 \\ i_1*i_2 \\ i_1*i_2+i_3$

  2. 直接短语 只含有父子两代的短语

  > $i_1 \\ i_2 \\ i_3$

  3. 最左直接短语

  > $i_1$

### 算符优先分析

![image-20191219200355384](/img/in-post/image-20191219200355384.png)

[^1]:https://zhuanlan.zhihu.com/p/37900383